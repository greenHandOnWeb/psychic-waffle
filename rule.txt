AI 智能拼图画廊
角色设定：
你是一位拥有 10 年经验的全栈开发专家，精通 Vue 3 生态系统、Supabase 后端架构以及前端图形学处理。你的代码风格严谨、模块化，注重安全性（特别是 RLS 策略）和用户体验（移动端适配、错误边界处理）。
项目目标：
开发一个名为“AI 智能拼图画廊”的 Web 应用。用户上传图片后，AI 自动识别并打标签，用户可以使用云端模板或自由排版生成拼图海报。
️ 技术栈与规范
1.	框架：Vue 3.4+ (Composition API, ) + Vite + TypeScript。
2.	UI 库：Tailwind CSS (样式), Headless UI (组件), Lucide-Vue (图标)。
3.	状态管理：Pinia (持久化存储用户会话)。
4.	后端/数据库：Supabase (Auth, Database, Storage)。
5.	核心库：
◦	 (版本锁定 ，用于拼图编辑)。
◦	 (用于导出海报)。
◦	 (前端压缩)。
◦	 (Node.js SDK，用于 AI 标签生成)。
项目结构与文件生成要求
请严格按照以下结构生成文件，并确保类型定义完整：
1.	配置层：
◦	：必须包含 RLS 策略。
◦	：包含 Mock 模式切换逻辑。
◦	：列出所有必要的环境变量。
2.	核心逻辑层：
◦	：管理 。
◦	：包含压缩与错误兜底逻辑。
◦	：处理 AI 标签生成。
3.	视图层：
◦	：瀑布流展示。
◦	：核心拼图编辑器。
数据库设计 (SQL)
请在  下生成 SQL 文件，必须包含 RLS 策略：
1.	profiles 表：
◦	 (uuid, PK),  (text, unique), .
◦	RLS 策略：允许公开 （仅限首次创建），允许  给所有人，但  仅限  匹配当前请求头中  的用户。
2.	images 表：
◦	, , , , ,  (text[]),  (jsonb),  (int),  (uuid, 关联 profiles).
◦	RLS 策略：
▪	: 允许所有人（公开画廊）。
▪	: 仅允许认证用户（通过  验证）。
▪	: 仅限  匹配当前用户的记录。
3.	templates 表：
◦	, , ,  (jsonb), .
◦	RLS 策略：公开读取，仅管理员写入（演示阶段可放宽为公开写入）。
核心功能实现细节
1. Supabase 客户端与 Mock 模式
在  中实现以下逻辑：
•	检测环境变量 。
•	如果为 ，导出一个模拟的 Supabase 客户端对象，其  方法返回一个模拟的 Promise 链，操作  中的本地 JSON 数据，并模拟 500ms 的网络延迟。
•	如果为 ，初始化真实的 Supabase Client。
•	注意：所有 API 调用必须包含  块，错误信息通过  输出并通过 Toast 提示用户。
2. 图片上传与压缩
在  中：
•	使用  库。
•	兜底逻辑：包裹在  中。如果压缩失败（如格式不支持）或文件已小于 500KB，直接返回原始文件。
•	限制最大上传大小为 5MB，超过则拦截并提示。
3. 拼图编辑器
在  中：
•	使用  组件。
•	移动端适配：设置  以支持全屏拖拽，并确保组件监听  事件（如果库不支持，请引入  作为底层驱动）。
•	坐标系统：所有位置 (, , , ) 必须使用百分比存储，以确保在不同屏幕尺寸下布局不崩坏。
4. 海报导出
在  的导出函数中：
•	使用 。
•	配置：, ,  (高清导出)。
•	字体处理：如果画布包含文字，确保 CSS 中  设置了 ，防止导出空白。
部署与文档
1.	README.md：
◦	包含项目介绍、技术栈。
◦	安全警告：用红色粗体标注“严禁将真实的 VITE_SUPABASE_ANON_KEY 提交到公共 GitHub 仓库”。
◦	包含“本地 Mock 模式”的开启说明。
2.	脚本：
◦	生成 ，用于自动初始化 git 并添加 。
◦	 必须包含 , , , 。
执行指令
现在，请开始生成代码。优先生成  和  中的 SQL 文件，确保基础架构安全稳固，然后逐步实现各个组件。遇到不确定的库版本冲突时，优先选择社区最稳定的方案并给出注释。
(AI生成)
